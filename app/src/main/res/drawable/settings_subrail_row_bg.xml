<?xml version="1.0" encoding="utf-8"?>
<!--
  Sub-rail row background.

  Three visual states:
    - selected:  filled pill in colorSecondaryContainer (active section)
    - pressed:   colorSurfaceContainerHigh (touch feedback)
    - default:   transparent (sub-rail background bleeds through)

  Corner radius 16dp matches the M3 large rounding the rail container uses.
  Colors use ?attr tokens so light/dark mode flips automatically.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSecondaryContainer" />
            <corners android:radius="16dp" />
        </shape>
    </item>

    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSurfaceContainerHigh" />
            <corners android:radius="16dp" />
        </shape>
    </item>

    <item android:state_hovered="true">
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorSurfaceContainerHigh" />
            <corners android:radius="16dp" />
        </shape>
    </item>

    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="16dp" />
        </shape>
    </item>
</selector>
