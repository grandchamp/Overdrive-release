<?xml version="1.0" encoding="utf-8"?>
<!--
  Theme-aware pill background: small rounded badge that holds a step
  number (or icon). Tonal primary container so it reads as branded
  emphasis on top of the dialog's surfaceContainerHigh fill.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorPrimaryContainer" />
    <corners android:radius="14dp" />
</shape>
